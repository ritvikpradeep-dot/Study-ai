export default function ForgotPasswordPage() {
  return (
    <div className="mx-auto flex max-w-md flex-col gap-4 px-6 py-16 text-center">
      <h1 className="text-2xl font-semibold tracking-tight">Password reset</h1>
      <p className="text-sm opacity-70">
        Password reset via email isn&apos;t wired up yet in this preview build — it&apos;s on the
        roadmap. For now, ask whoever set up this instance to reset it directly in the database.
      </p>
    </div>
  );
}
